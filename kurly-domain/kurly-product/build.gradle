dependencies {
    implementation project(':kurly-support')
    implementation project(":kurly-domain:kurly-db")
    implementation project(":kurly-domain:kurly-user")
}

bootJar.enabled=false
jar.enabled=true
