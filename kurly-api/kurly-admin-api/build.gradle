dependencies {
    implementation project(':kurly-domain') // delete
    implementation project(':kurly-core')
    implementation project(':kurly-internal:kurly-image')
}

bootJar.enabled=true
jar.enabled=false
